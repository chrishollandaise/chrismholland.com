---
export interface Props {
  isDraft: boolean
}

const { isDraft } = Astro.props
---

{
  isDraft && (
    <small class='text-orange-500 ml-2 rounded-lg  border-double align-top'>
      <code>draft</code>
    </small>
  )
}
