---
export interface Props {
  isDraft: boolean
}

const { isDraft } = Astro.props
---

{
  isDraft && (
    <small class='ml-2 rounded-lg border-double  align-top text-orange-500'>
      <code>draft</code>
    </small>
  )
}
