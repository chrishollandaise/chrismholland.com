---
export interface Props {
  isDraft: boolean
}

const { isDraft } = Astro.props
---

{
  isDraft ? (
    <small>
      <code class='bg-yellow-300 text-black'>draft</code>
    </small>
  ) : null
}
