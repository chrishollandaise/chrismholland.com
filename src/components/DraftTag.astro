---
export interface Props {
  isDraft: boolean
}

const { isDraft } = Astro.props
---

{
  isDraft ? (
    <small>
      <code style='color: black; background-color: lime;'>draft</code>
    </small>
  ) : null
}
